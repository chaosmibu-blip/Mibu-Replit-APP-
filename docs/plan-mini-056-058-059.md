# 計劃：MINI 貓咪系統 #056/#058/#059

> 建立日期：2026-02-25
> 狀態：已完成

## 背景

後端 sync-app.md 派發 #056-#061 共 6 個 MINI 貓咪系統任務，用戶選擇先做 #056、#058、#059。

- **#056** MINI Profile — 虛擬夥伴基本資料 + 改名
- **#058** 景點塗鴉牆 — 景點公開留言 CRUD（每人每景點最多 3 則）
- **#059** 景點筆記 — 私人筆記 CRUD（1-2000 字）

## 決策記錄

- #058/#059 整合進 PlaceDetailModal（加 Tab：詳情 | 塗鴉牆 | 筆記），而非獨立畫面
- #056 建立獨立的 MiniProfileScreen，透過 expo-router 路由 `app/mini-profile.tsx` 進入
- 使用現有 SegmentedControl 元件做 Tab 切換
- 塗鴉牆和筆記的 query hooks 以 placeId 為 key，跟 PlaceDetailModal 的 item.id 對應
- 型別定義一次涵蓋 #056-#061（已完成），API/hooks 只實作 #056-#059 範圍

## 執行項目

### Phase 1：基礎建設（型別 + API + Hooks）✅ 已完成
- [x] `src/types/mini.ts` — 全 MINI 系統型別定義
- [x] `src/services/miniApi.ts` — API 服務（#056-#059 端點）
- [x] `src/hooks/useMiniQueries.ts` — React Query hooks

### Phase 2：模組註冊 ✅ 已完成
- [x] `src/types/index.ts` — 加入 `export * from './mini'`
- [x] `src/services/api.ts` — 加入 miniApi import/re-export/proxy
- [x] `src/hooks/useAuthQuery.ts` — 加入 MINI query keys

### Phase 3：翻譯 ✅ 已完成
- [x] `src/constants/translations/zh-TW.ts` — 新增 mini_* keys
- [x] `src/constants/translations/en.ts` — 新增 mini_* keys
- [x] `src/constants/translations/ja.ts` — 新增 mini_* keys
- [x] `src/constants/translations/ko.ts` — 新增 mini_* keys

### Phase 4：PlaceDetailModal 改造（#058 + #059）✅ 已完成
- [x] 修改 `CollectionScreen.tsx` 的 PlaceDetailModal：
  - 加入 SegmentedControl（詳情 | 塗鴉牆 | 筆記）
  - 將現有內容包進 renderInfoTab()
  - 新增 renderGraffitiTab()（顯示留言列表 + 新增輸入框）
  - 新增 renderNotesTab()（顯示筆記列表 + 新增/編輯/刪除）
- [x] 新增 `CollectionScreen.styles.ts` 中的塗鴉牆/筆記相關樣式

### Phase 5：MiniProfileScreen（#056）✅ 已完成
- [x] 建立 `src/modules/traveler/screens/MiniProfileScreen.tsx`
  - 顯示：名字（可編輯）、心情（emoji + 翻譯）、飽食度（進度條）、羈絆等級、成長階段
  - 防重複觸發（useRef #006 教訓）、loading/error 狀態處理
- [x] 建立路由 `app/mini-profile.tsx`
- [x] 註冊 `src/modules/traveler/index.ts`

### Phase 6：收尾 ✅ 已完成
- [x] `npx tsc --noEmit` 零新增錯誤
- [x] 更新 `docs/sync-backend.md` 記錄 #056/#058/#059 完成
- [x] 更新 `docs/memory-screens.md` 記錄新畫面
- [x] Commit 完成

### Phase 7：品質修復（Code Review）✅ 已完成
- [x] CRITICAL: 拆分 noteInput 為 newNoteInput / editNoteInput，避免編輯時覆蓋新增內容
- [x] CRITICAL: ScrollView padding 移到 contentContainerStyle，新增 modalScrollView style
- [x] HIGH: 所有 mutation 加 onError callback（Alert 回饋）
- [x] HIGH: 按鈕 pending 時視覺狀態同步 disabled
- [x] MEDIUM: 切換 tab 時重設筆記編輯狀態
- [x] MEDIUM: note.tags null safety（?. 運算子）
- [x] MEDIUM: 塗鴉牆每人每景點 3 則上限前端檢查 + 友善提示
- [x] 硬編碼「在 Google 中查看」改用翻譯 key
- [x] MiniProfileScreen 改名失敗加 Alert 回饋
- [x] 新增 10 個錯誤提示翻譯 key（4 語系）
- [x] `npx tsc --noEmit` 零新增錯誤

## 注意事項

- 後端 API 尚未確認是否已部署（#007 教訓），hooks 層已完成但 UI 需要處理 API 錯誤 fallback
- PlaceDetailModal 的 item.id 即 placeId，可直接傳給 graffiti/notes hooks
- GachaItemWithRead 額外有 placeId（string），但 GachaItem.id（number）才是地點 ID
- ~~塗鴉牆每人每景點最多 3 則，需顯示友善提示~~ ✅ 已實作前端檢查
- 筆記 1-2000 字限制，maxLength 已設定
