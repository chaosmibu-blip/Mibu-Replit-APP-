/**
 * Card 元件重新匯出
 *
 * 從 shared 模組重新匯出 Card 元件
 * 提供統一的 import 路徑
 *
 * 使用方式：
 * import { Card } from '@/components/ui/Card';
 * // 或
 * import Card from '@/components/ui/Card';
 */

export { default, default as Card } from '../../modules/shared/components/ui/Card';
