/**
 * SegmentedControl 元件重新匯出
 *
 * 從 shared 模組重新匯出 SegmentedControl 元件
 * 提供統一的 import 路徑
 *
 * SegmentedControl 是分段選擇器，用於在多個選項間切換
 * 類似 iOS 的 UISegmentedControl
 *
 * 使用方式：
 * import { SegmentedControl } from '@/components/ui/SegmentedControl';
 */

export { SegmentedControl } from '../../modules/shared/components/ui/SegmentedControl';
