/**
 * 管理員主控台頁面路由
 *
 * 對應 Screen: AdminDashboardScreen
 * 功能：系統管理員的主要操作介面，包含用戶管理、公告管理等
 */
import { AdminDashboardScreen } from '../src/screens/AdminDashboardScreen';

export default function AdminDashboard() {
  return <AdminDashboardScreen />;
}
