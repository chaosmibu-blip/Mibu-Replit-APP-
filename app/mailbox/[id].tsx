/**
 * 信箱詳情頁面路由 (#045)
 *
 * 動態路由：/mailbox/:id
 * 對應 Screen: MailboxDetailScreen（來自 traveler 模組）
 * 功能：顯示完整獎勵內容 + 領取
 */
import { MailboxDetailScreen } from '../../src/modules/traveler';

export default MailboxDetailScreen;
