/**
 * 景點圖鑑頁面路由
 *
 * 對應 Screen: CollectionScreen
 * 功能：顯示用戶收集的景點圖鑑，支援篩選和搜尋
 *
 * 注意：此 Tab 在 _layout.tsx 中設為 href: null（隱藏）
 */
import { CollectionScreen } from '../../src/screens/CollectionScreen';

export default CollectionScreen;
