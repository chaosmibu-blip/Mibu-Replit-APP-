/**
 * 通知歷史列表頁面路由
 *
 * 對應 Screen: NotificationListScreen（來自 shared 模組）
 * 功能：顯示推播通知歷史記錄（分頁、標記已讀）
 */
import { NotificationListScreen } from '../src/modules/shared';

export default NotificationListScreen;
