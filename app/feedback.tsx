/**
 * ⚠️ [需 EAS Build] 此頁面依賴 expo-image-manipulator、expo-image-picker、expo-application，
 *    這些套件在上次 production build 後才加入 package.json，
 *    目前 build 不包含這些 native 模組，進入此頁會閃退。
 *    需重新 eas build 才能正常使用，OTA 無法修復。
 */
import { FeedbackScreen } from '../src/modules/shared/screens/FeedbackScreen';
export default FeedbackScreen;
