/**
 * 審核等待頁面路由
 *
 * 對應 Screen: PendingApprovalScreen
 * 功能：商家或專員申請後，等待後台審核時顯示的頁面
 */
import { PendingApprovalScreen } from '../src/screens/PendingApprovalScreen';

export default PendingApprovalScreen;
